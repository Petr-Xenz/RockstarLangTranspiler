namespace RockstarLangTranspiler.Tokens
{
    public class BreakToken : Token
    {
        public BreakToken(int linePosition, int lineNumber, string value) : base(linePosition, lineNumber, value)
        {
        }
    }

}