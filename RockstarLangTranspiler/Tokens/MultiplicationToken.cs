namespace RockstarLangTranspiler.Tokens
{
    public class MultiplicationToken : CombiningToken
    {
        public MultiplicationToken(int linePosition, int lineNumber, string value) : base(linePosition, lineNumber, value)
        {
        }
    }
}