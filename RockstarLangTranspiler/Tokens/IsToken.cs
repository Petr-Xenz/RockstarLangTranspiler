namespace RockstarLangTranspiler.Tokens
{
    public class IsToken : Token
    {
        public IsToken(int linePosition, int lineNumber, string value) : base(linePosition, lineNumber, value)
        {
        }
    }
}